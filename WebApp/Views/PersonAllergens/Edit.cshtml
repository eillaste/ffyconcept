@model WebApp.ViewModels.PersonAllergenCreateEditViewModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>PersonAllergen</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @*<div class="form-group">
                <label asp-for="AppUserId" class="control-label"></label>
                <select asp-for="AppUserId" class="form-control" asp-items="ViewBag.AppUserId"></select>
                <span asp-validation-for="AppUserId" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <label asp-for="PersonAllergen.FoodItemId" class="control-label"></label>
                <select asp-for="PersonAllergen.FoodItemId" class="form-control" asp-items="Model.AllergenSelectList"></select>
                <span asp-validation-for="PersonAllergen.FoodItemId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PersonAllergen.Description" class="control-label"></label>
                <input asp-for="PersonAllergen.Description" class="form-control" />
                <span asp-validation-for="PersonAllergen.Description" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="PersonAllergen.Id" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
