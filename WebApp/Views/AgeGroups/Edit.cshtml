@model WebApp.ViewModels.AgeGroupCreateEditViewModel

@{
    ViewData["Title"] = @Resources.Views.Shared._Layout.Edit;
}

<h1>@Resources.Views.Shared._Layout.Edit</h1>

<h4>@Resources.Views.Shared._Layout.AgeGroup</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="AgeGroup.LowerBound" class="control-label"></label>
                <input asp-for="AgeGroup.LowerBound" class="form-control" />
                <span asp-validation-for="AgeGroup.LowerBound" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AgeGroup.UpperBound" class="control-label"></label>
                <input asp-for="AgeGroup.UpperBound" class="form-control" />
                <span asp-validation-for="AgeGroup.UpperBound" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="AgeGroup.Id" />
            <div class="form-group">
                <input type="submit" value="@Resources.Views.Shared._Layout.Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">@Resources.Views.Shared._Layout.Index</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
