@using WebApp.ViewModels
@model WebApp.ViewModels.TolerableUpperIntakeLevelCreateEditViewModel

@{
    ViewData["Title"] = @Resources.Views.Shared._Layout.Edit;
}

<h1>@Resources.Views.Shared._Layout.Edit</h1>

<h4>@Resources.Views.Shared._Layout.TolerableUpperIntakeLevel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="TolerableUpperIntakeLevel.NutrientId" class="control-label"></label>
                <select asp-for="TolerableUpperIntakeLevel.NutrientId" class="form-control" asp-items="Model.NutrientSelectList"></select>
                <span asp-validation-for="TolerableUpperIntakeLevel.NutrientId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="TolerableUpperIntakeLevel.Quantity" class="control-label"></label>
                <input asp-for="TolerableUpperIntakeLevel.Quantity" class="form-control" />
                <span asp-validation-for="TolerableUpperIntakeLevel.Quantity" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="TolerableUpperIntakeLevel.Id" />
            <div class="form-group">
                <input type="submit" value="@Resources.Views.Shared._Layout.Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">@Resources.Views.Shared._Layout.Index</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
