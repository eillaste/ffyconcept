@model WebApp.ViewModels.DietCreateEditViewModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Diet</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Diet.Title" class="control-label"></label>
                <input asp-for="Diet.Title" class="form-control" />
                <span asp-validation-for="Diet.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Diet.Description" class="control-label"></label>
                <input asp-for="Diet.Description" class="form-control" />
                <span asp-validation-for="Diet.Description" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="Diet.Id" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
