@model WebApp.ViewModels.NutrientInFoodItemCreateEditViewModel

@{
    ViewData["Title"] = @Resources.Views.Shared._Layout.Edit;
}

<h1>@Resources.Views.Shared._Layout.Edit</h1>

<h4>@Resources.Views.Shared._Layout.NutrientInFoodItem</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="NutrientInFoodItem.NutrientId" class="control-label"></label>
                <select asp-for="NutrientInFoodItem.NutrientId" class="form-control" asp-items="Model.NutrientSelectList"></select>
                <span asp-validation-for="NutrientInFoodItem.NutrientId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="NutrientInFoodItem.FoodItemId" class="control-label"></label>
                <select asp-for="NutrientInFoodItem.FoodItemId" class="form-control" asp-items="Model.FoodItemSelectList"></select>
                <span asp-validation-for="NutrientInFoodItem.FoodItemId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="NutrientInFoodItem.Quantity" class="control-label"></label>
                <input asp-for="NutrientInFoodItem.Quantity" class="form-control" />
                <span asp-validation-for="NutrientInFoodItem.Quantity" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="NutrientInFoodItem.Id" />
            <div class="form-group">
                <input type="submit" value="@Resources.Views.Shared._Layout.Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">@Resources.Views.Shared._Layout.Index</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
