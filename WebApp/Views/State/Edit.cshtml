@model WebApp.ViewModels.StateCreateEditViewModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>State</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="State.StateTitle" class="control-label"></label>
                <input asp-for="State.StateTitle" class="form-control" />
                <span asp-validation-for="State.StateTitle" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="State.ModificationTime" class="control-label"></label>
                <input asp-for="State.ModificationTime" class="form-control" />
                <span asp-validation-for="State.ModificationTime" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="State.Id" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
