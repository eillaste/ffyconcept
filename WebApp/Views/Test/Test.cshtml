@model WebApp.ViewModels.Test.TestViewModel
 @*@model IEnumerable<BLL.App.DTO.StandardUnit>*@

@{
    ViewData["Title"] = "Test";
}

<h1>Test</h1>

@*<p>
    <a asp-action="Create">@Resources.Views.Shared._Layout.Create</a>
</p>*@
<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StandardUnits.FirstOrDefault()!.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StandardUnits.FirstOrDefault()!.Symbol)
        </th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model.StandardUnits) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Symbol)
            </td>
            @*<td>
                <a asp-action="Edit" asp-route-id="@item.Id">@Resources.Views.Shared._Layout.Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">@Resources.Views.Shared._Layout.Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">@Resources.Views.Shared._Layout.Delete</a>
            </td>*@
        </tr>
    }
    </tbody>
</table>
