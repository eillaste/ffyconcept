@model WebApp.ViewModels.HealthSpecificationCreateEditViewModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>HealthSpecification</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="HealthSpecification.Title" class="control-label"></label>
                <input asp-for="HealthSpecification.Title" class="form-control" />
                <span asp-validation-for="HealthSpecification.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="HealthSpecification.Description" class="control-label"></label>
                <input asp-for="HealthSpecification.Description" class="form-control" />
                <span asp-validation-for="HealthSpecification.Description" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="HealthSpecification.Id" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
